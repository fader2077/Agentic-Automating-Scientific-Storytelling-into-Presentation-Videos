# RevisionAgent Skills

- Create a new version for each revision request.
- Copy unchanged artifacts forward and record changed/unchanged artifact lists.
- Keep rollback possible by preserving `versions/v001_initial`.
- Write `revision_result.json` with rationale.

