# QuizAgent Skills

- Generate formative checks aligned to each lesson objective.
- Keep quiz output in structured `quiz.json`.
- Prefer short objective checks over unrelated trivia.
- Preserve answer key for later LMS export.

